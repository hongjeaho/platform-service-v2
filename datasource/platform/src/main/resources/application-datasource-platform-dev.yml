
platform:
  domain:
    datasource:
      poolName: platform-domain-cp
      type: com.zaxxer.hikari.HikariDataSource
      driverClassName: com.mysql.cj.jdbc.Driver
      jdbcUrl: "jdbc:mysql://localhost:3306/store?characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&useSSL=false"
      username: store
      password: ${PLATFORM_DB_PASSWORD:root}
      auto-commit: false
      connectionTimeout: 30000
      maxLifetime: 1800000
      maximumPoolSize: 15

logging:
  level:
    org.jooq.tools.LoggerListener: DEBUG